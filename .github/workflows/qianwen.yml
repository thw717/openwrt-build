name: Build OpenWrt x86_64

on:
  workflow_dispatch:    # 唯一触发方式：Actions 页面手动点击 Run workflow

env:
  OPENWRT_REPO:     openwrt/openwrt
  OPENWRT_VERSION:  v25.12.5
  SONIC_SCRIPT_URL: https://raw.githubusercontent.com/mufeng05/openwrt-sonic-fullcone/master/add_sonic_fullcone.sh
  SRC_DIR:          openwrt-src
  BIN_DIR:          bin/targets/x86/64

concurrency:
  group: build-openwrt-x86-64
  cancel-in-progress: true

jobs:
  build:
    name: x86_64
    runs-on: ubuntu-22.04
    timeout-minutes: 360

    steps:
      - name: Free disk space
        run: |
          sudo swapoff -a && sudo rm -f /swapfile
          sudo apt clean
          df -h

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y build-essential ccache ecj fastjar file g++ \
            gawk gettext git java-propose-classpath libelf-dev libncurses5-dev \
            libncursesw5-dev libssl-dev python3 python3-dev python3-distutils \
            rsync unzip wget zlib1g-dev xsltproc time patch bash

      - name: Checkout OpenWrt
        uses: actions/checkout@v6
        with:
          repository: ${{ env.OPENWRT_REPO }}
          ref: ${{ env.OPENWRT_VERSION }}
          fetch-depth: 1
          path: ${{ env.SRC_DIR }}

      - name: Update feeds
        working-directory: ${{ env.SRC_DIR }}
        run: |
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Apply sonic fullcone patches
        working-directory: ${{ env.SRC_DIR }}
        run: |
          curl -sSL ${{ env.SONIC_SCRIPT_URL }} | bash

      - name: Write .config
        working-directory: ${{ env.SRC_DIR }}
        run: |
          cat > .config <<'EOF'
CONFIG_TARGET_x86=y
CONFIG_TARGET_x86_64=y
CONFIG_TARGET_x86_64_DEVICE_generic=y
CONFIG_DEVEL=y
CONFIG_CCACHE=y

CONFIG_PACKAGE_luci=y
CONFIG_PACKAGE_luci-i18n-base-zh-cn=y
CONFIG_PACKAGE_luci-i18n-firewall-zh-cn=y

CONFIG_PACKAGE_luci-app-ttyd=y
CONFIG_PACKAGE_miniupnpd-nftables=y
CONFIG_PACKAGE_luci-app-upnp=y
CONFIG_PACKAGE_luci-i18n-upnp-zh-cn=y

CONFIG_PACKAGE_ddns-scripts=y
CONFIG_PACKAGE_luci-app-ddns=y
CONFIG_PACKAGE_ddns-scripts-cloudflare=y
CONFIG_PACKAGE_ddns-scripts-dnspod=y
CONFIG_PACKAGE_luci-i18n-ddns-zh-cn=y

CONFIG_PACKAGE_luci-app-uhttpd=y
CONFIG_PACKAGE_luci-i18n-uhttpd-zh-cn=y

CONFIG_PACKAGE_luci-app-statistics=y
CONFIG_PACKAGE_collectd-mod-thermal=y
CONFIG_PACKAGE_kmod-thermal=y
CONFIG_PACKAGE_kmod-hwmon-core=y

CONFIG_PACKAGE_curl=y
CONFIG_PACKAGE_ca-certificates=y

CONFIG_PACKAGE_luci-app-sonic-fullcone=y
EOF

      - name: Defconfig & download
        working-directory: ${{ env.SRC_DIR }}
        run: |
          make defconfig
          make download -j$(nproc) || make download -j1 V=s

      - name: Cache ccache & dl
        uses: actions/cache@v6
        with:
          path: |
            ~/.ccache
            ${{ env.SRC_DIR }}/dl
          key: ${{ runner.os }}-owrt-v25.12.5
          restore-keys: ${{ runner.os }}-owrt-

      - name: Build
        working-directory: ${{ env.SRC_DIR }}
        run: make -j$(nproc) || make -j1 V=s

      - name: Upload artifacts
        uses: actions/upload-artifact@v6
        if: always()
        with:
          name: OpenWrt-x86_64-${{ env.OPENWRT_VERSION }}
          path: |
            ${{ env.SRC_DIR }}/${{ env.BIN_DIR }}/*.img.gz
            ${{ env.SRC_DIR }}/${{ env.BIN_DIR }}/*.manifest
            ${{ env.SRC_DIR }}/${{ env.BIN_DIR }}/profiles.json
            ${{ env.SRC_DIR }}/${{ env.BIN_DIR }}/packages/
          retention-days: 7
          if-no-files-found: warn
