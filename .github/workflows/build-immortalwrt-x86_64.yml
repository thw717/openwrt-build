name: Build ImmortalWrt x86_64 Firmware

on:
  workflow_dispatch:
    inputs:
      source_branch:
        description: 'ImmortalWrt 源码分支'
        required: true
        default: 'openwrt-24.10'
        type: string

env:
  REPO_URL: https://github.com/immortalwrt/immortalwrt

jobs:
  build-firmware:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install compile dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential ccache ecj fastjar file g++ gawk \
          gettext git java-propose-classpath libelf-dev libncurses5-dev \
          libssl-dev libtool python3 python3-setuptools python3-pyelftools \
          rsync subversion swig time unzip wget xsltproc zlib1g-dev

      - name: Clone ImmortalWrt official source
        run: |
          git clone --depth 1 --branch ${{ github.event.inputs.source_branch }} $REPO_URL immortalwrt
          cd immortalwrt
          echo "BUILD_TAG=$(date +%Y%m%d)" >> $GITHUB_ENV

      - name: Clone DDNSTO package (直接克隆到package目录)
        run: |
          cd immortalwrt/package
          git clone --depth 1 https://github.com/linkease/ddnsto-openwrt-package.git ddnsto

      - name: Update & install feeds
        run: |
          cd immortalwrt
          # 清理冲突环境变量 + 失败自动重试
          unset ARCH CFLAGS CPPFLAGS CXXFLAGS
          ./scripts/feeds update -a || ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Generate custom .config
        run: |
          cd immortalwrt
          cat > .config << 'EOF'
          # ======================== 目标平台配置 ========================
          CONFIG_TARGET_x86=y
          CONFIG_TARGET_x86_64=y
          CONFIG_TARGET_x86_64_DEVICE_generic=y

          # ======================== 固件镜像格式 ========================
          CONFIG_TARGET_ROOTFS_SQUASHFS=y
          CONFIG_TARGET_ROOTFS_EXT4FS=y
          CONFIG_GRUB_EFI_IMAGES=y
          CONFIG_GRUB_TIMEOUT="1"

          # ======================== 基础依赖组件 ========================
          CONFIG_PACKAGE_luci=y
          CONFIG_PACKAGE_luci-base=y
          CONFIG_PACKAGE_curl=y
          CONFIG_PACKAGE_ca-certificates=y

          # ======================== 核心中文语言包 ========================
          CONFIG_PACKAGE_luci-i18n-base-zh-cn=y
          CONFIG_PACKAGE_luci-i18n-firewall-zh-cn=y

          # ======================== TTYD 网页终端 ========================
          CONFIG_PACKAGE_luci-app-ttyd=y

          # ======================== UPnP (nftables 版) ========================
          CONFIG_PACKAGE_miniupnpd-nftables=y
          CONFIG_PACKAGE_luci-app-upnp=y
          CONFIG_PACKAGE_luci-i18n-upnp-zh-cn=y

          # ======================== DDNS 脚本版 ========================
          CONFIG_PACKAGE_ddns-scripts=y
          CONFIG_PACKAGE_luci-app-ddns=y
          CONFIG_PACKAGE_ddns-scripts-cloudflare=y
          CONFIG_PACKAGE_ddns-scripts-dnspod=y
          CONFIG_PACKAGE_luci-i18n-ddns-zh-cn=y

          # ======================== uHTTPd 管理界面 ========================
          CONFIG_PACKAGE_luci-app-uhttpd=y
          CONFIG_PACKAGE_luci-i18n-uhttpd-zh-cn=y

          # ======================== DDNS-Go ========================
          CONFIG_PACKAGE_ddns-go=y
          CONFIG_PACKAGE_luci-app-ddns-go=y
          CONFIG_PACKAGE_luci-i18n-ddns-go-zh-cn=y

          # ======================== DDNSTO ========================
          CONFIG_PACKAGE_ddnsto=y
          CONFIG_PACKAGE_luci-app-ddnsto=y
          CONFIG_PACKAGE_luci-i18n-ddnsto-zh-cn=y
          EOF

      - name: Auto-complete build config
        run: |
          cd immortalwrt
          unset ARCH CFLAGS CPPFLAGS CXXFLAGS
          # 显式指定正确架构 + 输出详细日志
          make ARCH=x86 defconfig V=s

      - name: Download source packages
        run: |
          cd immortalwrt
          unset ARCH CFLAGS CPPFLAGS CXXFLAGS
          make ARCH=x86 download -j8

      - name: Compile firmware
        run: |
          cd immortalwrt
          unset ARCH CFLAGS CPPFLAGS CXXFLAGS
          # 多线程优先，失败自动降级单线程输出详细日志
          make ARCH=x86 -j$(nproc) || make ARCH=x86 -j1 V=s

      - name: Organize output files
        run: |
          cd immortalwrt
          mkdir -p output/firmware output/config
          cp -r bin/targets/x86/64/* output/firmware/
          cp .config output/config/build.config
          echo "Build completed: ${{ env.BUILD_TAG }}"

      - name: Upload firmware artifacts
        uses: actions/upload-artifact@v6
        with:
          name: ImmortalWrt-x86_64-${{ env.BUILD_TAG }}
          path: immortalwrt/output/
          retention-days: 30
